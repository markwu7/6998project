import React, { useState } from 'react';
import { useNavigate, useLocation } from 'react-router-dom';
import axios from 'axios';

const Whole = () => {
    const location = useLocation();
    const recs = location.recs;
    
};

export default Whole;